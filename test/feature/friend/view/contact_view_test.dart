import 'package:flutter_test/flutter_test.dart';
import 'package:iamhere/feature/friend/view/contact_view.dart';

void main() {
  group('ContactView', () {
    testWidgets('compiles and builds', (WidgetTester tester) async {
      expect(ContactView, isA<Type>());
    });
  });
}
